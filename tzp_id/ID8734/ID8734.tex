\section*{ID8734: Relation: F(x,t) := A(x,t),P(x,t)}
\paragraph{Metadata.}
PiID: 0333466974992 | RTEID: RTE:P33916.0616:T5.6178 | UUID: 181aff05-9b4a-52e1-b668-a3bdcc932101

\paragraph{Canonical Statement.}
\$\textbackslash{}mathbf\{F\}(x,t) := A(x,t),P(x,t)\$ , a weighted scalar field. If needed, we could consider \$\textbackslash{}mathbf\{F\}\$ as a vector by \$A\$ being scalar and \$P\$ scalar, their product is scalar; but here we treat it as input to curl, so implicitly we might consider \$A P\$ as multiplying an identity vector field or something. More rigorously, one might embed \$P\$ in a vector potential form \$\textbackslash{}mathbf\{F\} = ∇\textbackslash{}Phi\$ or similar, but for now consider \$\textbackslash{}mathbf\{F\} = (0,0,A,P)\$ just to feed something into a curl.

\paragraph{Canonical Equation.}
\[
\mathbf{F}(x,t) := A(x,t),P(x,t)
\]

\paragraph{Dictionary.}
Relation: F(x,t) := A(x,t),P(x,t) — F(x,t) := A(x,t),P(x,t)

\paragraph{Encyclopedia.}
Relation: F(x,t) := A(x,t),P(x,t) is an algebraic definition, written F(x,t) := A(x,t),P(x,t). It is an algebraic definition expressing the quantity directly in terms of the variables on its right-hand side. It is built from F, x, A, P. Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
