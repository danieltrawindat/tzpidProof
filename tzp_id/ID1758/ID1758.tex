\section*{ID1758: Elasser}
\paragraph{Metadata.}
PiID: 7904946016534 | RTEID: RTE:P6826.7044:T4.9651 | UUID: 92730183-f4e4-5cf3-8ead-6a7fbf6bdde1

\paragraph{Canonical Statement.}
The blocked Elasser continuation receives the same verified Green-function expansion as ID1740, preserving duplicate lineage while giving the folder a concrete mathematical packet.

\paragraph{Canonical Equation.}
\[
G(\mathbf r,\mathbf r';\mu_r)=\frac{1}{4\pi\lvert\mathbf r-\mathbf r'\rvert}+\sum_{\ell,m}a_{\ell m}(\mu_r)Y_{\ell}^{m}(\theta,\phi)\left(\frac{r_{<}}{R}\right)^{\ell}\left(\frac{R}{r_{>}}\right)^{\ell+1}
\]

\paragraph{Dictionary.}
Technically, Elasser is interpreted through the canonical variables in the repaired equation and through the adjacent progression terms retained in the packet (G(mathbf r,mathbf r';μ\_r)) [ID1758]

\paragraph{Encyclopedia.}
Technically, Elasser is interpreted through the canonical variables in the repaired equation and through the adjacent progression terms retained in the packet.
