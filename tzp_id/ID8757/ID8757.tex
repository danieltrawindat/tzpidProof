\section*{ID8757: Relation: (π/2,Φ; -1)}
\paragraph{Metadata.}
PiID: 7802490114195 | RTEID: RTE:P34005.3757:T5.7649 | UUID: 1209a703-567c-5d9e-95e2-be7e883292d5

\paragraph{Canonical Statement.}
orth geographic pole direction might be labeled \$(\textbackslash{}Theta=0, \textbackslash{}Upsilon=-1)\$ if we consider field lines entering there (i.e., north magnetic pole is flux entering Earth), and the south geographic pole would be \$(\textbackslash{}Theta=\textbackslash{}pi, \textbackslash{}Upsilon=+1)\$ if flux emerges. All along the equator ( \$\textbackslash{}Theta = \textbackslash{}pi/2\$ , \$0 \textbackslash{}le \textbackslash{}Phi < 2\textbackslash{}pi\$ ), field lines from TZP⁺ and TZP⁻ mix, but we can label each direction twice: once as \$(\textbackslash{}pi/2,\textbackslash{}Phi; +1)\$ meaning the direction on the + sheet (coming from TZP⁺ originally), and \$(\textbackslash{}pi/2,\textbackslash{}Phi; -1)\$ meaning the same spatial direction but corresponding to field that came from TZP⁻. In free space far from TZP, field from both branches might both be present in the same spatial direction (for instance, if fields overlap, or if considering wave interference etc.), but conceptually, one can separate them by tracking which branch they belong to. The necessity of DAANSsphere becomes even clearer if

\paragraph{Canonical Equation.}
\[
(\pi/2,\Phi; -1)
\]

\paragraph{Dictionary.}
Relation: (π/2,Φ; -1) — (π/2,Φ; -1)

\paragraph{Encyclopedia.}
Relation: (π/2,Φ; -1) is a symbolic expression, written (π/2,Φ; -1). It introduces a symbol or relation used elsewhere in the framework. It is built from π, Φ. Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
