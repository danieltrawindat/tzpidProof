\section*{ID9911: Design Metrics and Entanglement Measures: S\_p(θ)}
\paragraph{Metadata.}
PiID: 7428802109276 | RTEID: RTE:P38486.6140:T0.5823 | UUID: b737166a-881d-5352-a3f2-172dfd46a046

\paragraph{Canonical Statement.}
Given a kernel \$K\_p\$, one can define standard entanglement measures. For example, the von Neumann entanglement entropy \textbackslash{}[ S\_p(\textbackslash{}theta) = -\textbackslash{}mathrm\{Tr\}\textbackslash{}bigl(\textbackslash{}rho\_p\textasciicircum{}\{(1)\}(\textbackslash{}theta) \textbackslash{}log \textbackslash{}rho\_p\textasciicircum{}\{(1)\}(\textbackslash{}theta) \textbackslash{}bigr), \textbackslash{}] and for Gaussian or approximately Gaussian states, species-dependent concurrence or logarithmic negativity can be used. For design purposes, we collect these into a vector of metrics \textbackslash{}[ \textbackslash{}bm\{E\}\_p(\textbackslash{}theta) = \textbackslash{}bigl(S\_p(\textbackslash{}theta), \textbackslash{}mathcal\{C\}\_p(\textbackslash{}theta), \textbackslash{}mathcal\{N\}\_p(\textbackslash{}theta), \textbackslash{}dots\textbackslash{}bigr), \textbackslash{}] which we will attempt to match to target values by adjusting meta-atom geometries and compositions.

\paragraph{Canonical Equation.}
\[
S_p(\theta) = -\mathrm{Tr}\bigl(\rho_p^{(1)}(\theta) \log \rho_p^{(1)}(\theta) \bigr),
\]

\paragraph{Dictionary.}
Given a kernel K\_p, one can define standard entanglement measures (S\_p(θ))

\paragraph{Encyclopedia.}
Design Metrics and Entanglement Measures: S\_p(θ) is an expectation / trace over state, written S\_p(θ) = -Tr l(ρ\_p\textasciicircum{}(1)(θ) log ρ\_p\textasciicircum{}(1)(θ) r). It is computed as an expectation value or trace over a quantum state, producing an observable. It is built from θ, p, defining S\_p(θ). Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
