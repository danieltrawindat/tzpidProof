\section*{ID8763: Definition of ρ\_+(n)}
\paragraph{Metadata.}
PiID: 0114195212382 | RTEID: RTE:P34028.6751:T5.8033 | UUID: 3461ef8b-5be6-5e77-9750-e61d9ade392b

\paragraph{Canonical Statement.}
want to do calculus on this, we can think in terms of covering space: \$\textbackslash{}mathcal\{S\} \{DAANS\}\$ is a 2-sheeted cover of \$S\textasciicircum{}2\$ . Now, how does \$\textbackslash{}mathcal\{S\} \{DAANS\}\$ help in the theory? Primarily, it allows us to define fields and integrals that respect the branch separation. For example, we could have a branch-specific density \$\textbackslash{}rho +(\textbackslash{}hat\{n\})\$ meaning how much flux goes out in direction \$\textbackslash{}hat\{n\}\$ from TZP, and \$\textbackslash{}rho\_-(\textbackslash{}hat\{n\})\$ for flux coming in. If the field is static, one might have \$\textbackslash{}rho\_+(\textbackslash{}hat\{n\}) = \textbackslash{}rho\_-(\textbackslash{}hat\{n\})\$ by symmetry (just opposite sign if defining flux direction). But in general, say if TZP were emitting a pulse more strongly in one direction, \$\textbackslash{}rho\_+\$ on that side might spike, while \$\textbackslash{}rho\_-\$ might remain baseline. Furthermore, DAANSsphere coordinates allow imposing a closure condition easily: physically, all flux leaving TZP⁺ must enter TZP⁻ somewhere (since the loop is closed). That me

\paragraph{Canonical Equation.}
\[
\rho_+(\hat{n}) = \rho_-(\hat{n})
\]

\paragraph{Dictionary.}
Definition of ρ\_+(n) — ρ\_+(n) = ρ\_-(n)

\paragraph{Encyclopedia.}
Definition of ρ\_+(n) is an expectation / trace over state, written ρ\_+(n) = ρ\_-(n). It is computed as an expectation value or trace over a quantum state, producing an observable. It is built from n, defining ρ\_+(n). Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
