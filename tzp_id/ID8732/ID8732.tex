\section*{ID8732: Definition of Q(x,t) := I[ρ]}
\paragraph{Metadata.}
PiID: 2103334669749 | RTEID: RTE:P33908.2952:T5.6050 | UUID: 6183a8b8-17e4-545e-9b79-9cb776b7d82c

\paragraph{Canonical Statement.}
\$Q(x,t) := I[ρ] = \textbackslash{}int \textbackslash{}rho(x,t),dV\$ , a scalar field (or global scalar if truly integrated over all space).

\paragraph{Canonical Equation.}
\[
Q(x,t) := I[ρ] = \int \rho(x,t),dV
\]

\paragraph{Dictionary.}
Q(x,t) := I[ρ] = ∫ ρ(x,t),dV , a scalar field (or global scalar if truly integrated over all space).

\paragraph{Encyclopedia.}
a scalar field (or global scalar if truly integrated over all space). It is obtained by integrating over a domain, accumulating contributions across the region of interest.
