\section*{ID7528: To Find the Raw Outputs: r\_rm Orbit(t)}
\paragraph{Metadata.}
PiID: 5386076482363 | RTEID: RTE:P29232.8958:T4.1845 | UUID: d1bb6e32-f11c-54c2-b536-1a8eecc7f3cc

\paragraph{Canonical Statement.}
\# Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_\{\textbackslash{}rm DSS\}\textbackslash{}) in AU. - Four plots: 1. orbital coherence \textbackslash{}(r\_\{\textbackslash{}rm orbit\}(t)\textbackslash{}) vs time (years), 2. spin coherence \textbackslash{}(r\_\{\textbackslash{}rm spin\}(t)\textbackslash{}) vs time (years), 3. final orbital phase histogram, 4. final spin phase histogram. - A short summary table with final/max coherence numbers.

\paragraph{Canonical Equation.}
\[
r_{\rm orbit}(t)
\]

\paragraph{Dictionary.}
Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_ DSS\textbackslash{}) in AU [ID7528]

\paragraph{Encyclopedia.}
To Find the Raw Outputs: r\_rm Orbit(t) is a symbolic expression, written r\_rm orbit(t). It introduces a symbol or relation used elsewhere in the framework. It is built from r\_ orbit. Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
