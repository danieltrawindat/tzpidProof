\section*{ID7527: To Find the Raw Outputs: a\_rm DSS}
\paragraph{Metadata.}
PiID: 2538607648236 | RTEID: RTE:P29229.0126:T4.1781 | UUID: 8141ef58-0acd-537a-bd78-729fa456d259

\paragraph{Canonical Statement.}
\# Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_\{\textbackslash{}rm DSS\}\textbackslash{}) in AU. - Four plots: 1. orbital coherence \textbackslash{}(r\_\{\textbackslash{}rm orbit\}(t)\textbackslash{}) vs time (years), 2. spin coherence \textbackslash{}(r\_\{\textbackslash{}rm spin\}(t)\textbackslash{}) vs time (years), 3. final orbital phase histogram, 4. final spin phase histogram. - A short summary table with final/max coherence numbers.

\paragraph{Canonical Equation.}
\[
a_{\rm DSS}
\]

\paragraph{Dictionary.}
Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_ DSS\textbackslash{}) in AU.

\paragraph{Encyclopedia.}
To Find the Raw Outputs: a\_rm DSS is a symbolic expression, written a\_rm DSS. It introduces a symbol or relation used elsewhere in the framework. It is built from a\_ DSS. Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
