\section*{ID7529: To Find the Raw Outputs: r\_rm Spin(t)}
\paragraph{Metadata.}
PiID: 3860764823634 | RTEID: RTE:P29236.7790:T4.1909 | UUID: c3390041-b456-5c05-8ace-7eb942bc6e12

\paragraph{Canonical Statement.}
\# Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_\{\textbackslash{}rm DSS\}\textbackslash{}) in AU. - Four plots: 1. orbital coherence \textbackslash{}(r\_\{\textbackslash{}rm orbit\}(t)\textbackslash{}) vs time (years), 2. spin coherence \textbackslash{}(r\_\{\textbackslash{}rm spin\}(t)\textbackslash{}) vs time (years), 3. final orbital phase histogram, 4. final spin phase histogram. - A short summary table with final/max coherence numbers.

\paragraph{Canonical Equation.}
\[
r_{\rm spin}(t)
\]

\paragraph{Dictionary.}
Where to find the raw outputs During the run I produced: - A pivot table (rows = object radius in km; cols = T\_sys in years) showing \textbackslash{}(a\_ DSS\textbackslash{}) in AU [ID7529]

\paragraph{Encyclopedia.}
To Find the Raw Outputs: r\_rm Spin(t) is a symbolic expression, written r\_rm spin(t). It introduces a symbol or relation used elsewhere in the framework. It is built from r\_ spin. Within the Trawin Zero Point registry it serves as one element of the framework's mathematical narrative.
